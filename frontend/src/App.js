import { useEffect, useMemo, useState } from "react";
import axios from "axios";
import { LayoutDashboard, Package, Users, ShoppingBag, LogOut, Plus, Search, ArrowUpRight, Truck, MapPin, ChevronDown, Check, X } from "lucide-react";
import "@/App.css";
import "@/fixes.css";

const API = `${process.env.REACT_APP_BACKEND_URL}/api`;
const money = (n) => `₹${Number(n).toLocaleString("en-IN")}`;

function Login({ onLogin }) { const [phone, setPhone] = useState(""); const [otp, setOtp] = useState(""); const [sent, setSent] = useState(false); const [error, setError] = useState("");
  const send = async () => { if(phone.length < 10) return setError("Enter a valid 10-digit phone number"); await axios.post(`${API}/auth/request-otp`, {phone}); setSent(true); setError(""); };
  const verify = async () => { try { const r = await axios.post(`${API}/auth/verify-otp`, {phone, otp}); onLogin(r.data.seller); } catch(e) { setError(e.response?.data?.detail || "Please check your OTP"); } };
  return <main className="login-page"><div className="login-art"><div className="brand-mark">grove<span>+</span></div><div className="art-copy"><p className="eyebrow">SELLER OPERATIONS</p><h1>Your neighborhood,<br/><em>beautifully stocked.</em></h1><p>One calm workspace for every customer, product and order in your society.</p></div><div className="art-note"><span>✦</span> Trusted by local sellers</div></div><section className="login-box"><div className="mobile-brand brand-mark">grove<span>+</span></div><p className="eyebrow">WELCOME BACK</p><h2>Sign in to your<br/><strong>seller workspace</strong></h2><p className="muted">Use your registered phone number to continue.</p><label>PHONE NUMBER</label><div className="phone-input"><span>+91</span><input data-testid="seller-phone-input" value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g, "").slice(0,10))} placeholder="98765 43210"/></div>{sent && <><label>ONE-TIME PASSWORD</label><input data-testid="seller-otp-input" className="otp-input" value={otp} onChange={e=>setOtp(e.target.value.slice(0,6))} placeholder="Enter 6-digit OTP"/><p className="demo-hint" data-testid="demo-otp-hint">Demo OTP: <b>123456</b></p></>}{error && <p className="error" data-testid="seller-login-error">{error}</p>}<button data-testid="seller-login-btn" className="primary-btn login-btn" onClick={sent ? verify : send}>{sent ? "Verify & enter" : "Send OTP"}<ArrowUpRight size={17}/></button><p className="fine-print">By continuing, you agree to Grove's seller terms.</p></section></main>;
}

const initialForm = {name:"",category:"Dairy",price:"",stock:"",available:true};
function App() { const [seller,setSeller]=useState(()=>JSON.parse(localStorage.getItem("seller")||"null")); const [tab,setTab]=useState("Overview"); const [products,setProducts]=useState([]); const [customers,setCustomers]=useState([]); const [orders,setOrders]=useState([]); const [loading,setLoading]=useState(true); const [showProduct,setShowProduct]=useState(false); const [showCustomer,setShowCustomer]=useState(false); const [editingProduct,setEditingProduct]=useState(null); const [editingCustomer,setEditingCustomer]=useState(null); const [form,setForm]=useState(initialForm); const [search,setSearch]=useState(""); const [selected,setSelected]=useState(""); const [cart,setCart]=useState({}); const [society,setSociety]=useState("All societies");
  const load=async()=>{ try { const [p,c,o]=await Promise.all([axios.get(`${API}/products`),axios.get(`${API}/customers`),axios.get(`${API}/orders`)]); setProducts(p.data);setCustomers(c.data);setOrders(o.data); } finally { setLoading(false); } }; useEffect(()=>{if(seller)load();else setLoading(false)},[seller]); if(!seller) return <Login onLogin={s=>{localStorage.setItem("seller",JSON.stringify(s));setSeller(s);setLoading(true)}}/>; if(loading) return <div className="loading-screen" data-testid="workspace-loading"><div className="loading-mark">grove<span>+</span></div><p>Preparing your workspace…</p></div>;
  const saveProduct=async()=>{const data={...form,price:+form.price,stock:+form.stock};if(editingProduct) await axios.put(`${API}/products/${editingProduct.id}`,data);else await axios.post(`${API}/products`,data);setShowProduct(false);setEditingProduct(null);setForm(initialForm);load()}; const saveCustomer=async()=>{if(editingCustomer) await axios.put(`${API}/customers/${editingCustomer.id}`,form);else await axios.post(`${API}/customers`,form);setShowCustomer(false);setEditingCustomer(null);setForm(initialForm);load()}; const editProduct=(p)=>{setEditingProduct(p);setForm(p);setShowProduct(true)}; const editCustomer=(c)=>{setEditingCustomer(c);setForm(c);setShowCustomer(true)};
  const cartItems=Object.entries(cart).filter(([,q])=>q>0).map(([id,q])=>{const p=products.find(x=>x.id===id);return {...p,quantity:q}}); const total=cartItems.reduce((s,p)=>s+p.price*p.quantity,0);
  const placeOrder=async()=>{const c=customers.find(x=>x.id===selected);if(!c||!cartItems.length)return;await axios.post(`${API}/orders`,{customer_id:c.id,customer_name:c.name,society:c.society,items:cartItems.map(p=>({product_id:p.id,name:p.name,quantity:p.quantity,price:p.price})),total});setCart({});setSelected("");load();setTab("Orders")};
  const visibleProducts=products.filter(p=>p.name.toLowerCase().includes(search.toLowerCase())); const stats=[{label:"Revenue this month",value:money(orders.reduce((s,o)=>s+o.total,0)||28460),change:"+12.8%",icon:"₹"},{label:"Active products",value:products.length||4,change:"in catalogue",icon:"▦"},{label:"Customers served",value:customers.length||128,change:"across 5 societies",icon:"◎"}];
  return <div className="app-shell"><aside><div className="brand-mark">grove<span>+</span></div><p className="side-label">WORKSPACE</p>{[{n:"Overview",i:LayoutDashboard},{n:"Inventory",i:Package},{n:"Customers",i:Users},{n:"Orders",i:ShoppingBag}].map(({n,i:Icon})=><button data-testid={`nav-${n.toLowerCase()}`} className={tab===n?"nav active":"nav"} onClick={()=>setTab(n)}><Icon size={18}/>{n}{n==="Orders"&&orders.length>0&&<b>{orders.length}</b>}</button>)}<div className="side-bottom"><div className="seller-mini"><div className="avatar">AS</div><div><b>{seller.name}</b><small>Seller account</small></div></div><button data-testid="seller-logout-btn" className="logout" onClick={()=>{localStorage.removeItem("seller");setSeller(null)}}><LogOut size={16}/> Sign out</button></div></aside><main className="content"><header><div><p className="eyebrow">{tab === "Overview" ? "MONDAY, 24 JUNE 2024" : "GROVE / " + tab.toUpperCase()}</p><h1 data-testid="page-heading">{tab === "Overview" ? "Good morning, Ananya" : tab}</h1></div><div className="header-actions"><div className="society-pill"><MapPin size={16}/><span>All societies</span><ChevronDown size={14}/></div><div className="avatar">AS</div></div></header>{tab==="Overview"&&<Overview stats={stats} products={products} customers={customers} orders={orders} setTab={setTab}/>} {tab==="Inventory"&&<Inventory products={visibleProducts} search={search} setSearch={setSearch} onAdd={()=>{setEditingProduct(null);setForm(initialForm);setShowProduct(true)}} onEdit={editProduct}/>} {tab==="Customers"&&<Customers customers={customers} society={society} setSociety={setSociety} onAdd={()=>{setEditingCustomer(null);setForm(initialForm);setShowCustomer(true)}} onEdit={editCustomer}/>} {tab==="Orders"&&<Orders orders={orders} customers={customers} products={products} selected={selected} setSelected={setSelected} cart={cart} setCart={setCart} cartItems={cartItems} total={total} placeOrder={placeOrder} load={load}/>} {showProduct&&<Modal title={editingProduct?"Update product":"Add a product"} close={()=>{setShowProduct(false);setEditingProduct(null)}}><FormProduct form={form} setForm={setForm} submit={saveProduct}/></Modal>}{showCustomer&&<Modal title={editingCustomer?"Update customer":"Add a customer"} close={()=>{setShowCustomer(false);setEditingCustomer(null)}}><FormCustomer form={form} setForm={setForm} submit={saveCustomer}/></Modal>}</main></div>;
}

function Overview({stats,products,customers,orders,setTab}){return <><section className="welcome-band"><div><p className="eyebrow light">TODAY'S SNAPSHOT</p><h2>Small moments,<br/><em>big impact.</em></h2><p>Keep your society shelves full and your customers happy.</p></div><div className="band-image"/></section><div className="stats-grid">{stats.map(s=><div className="stat" data-testid={`stat-${s.label.toLowerCase().replaceAll(" ","-")}`}><div className="stat-icon">{s.icon}</div><small>{s.label}</small><strong>{s.value}</strong><span>{s.change}</span></div>)}</div><div className="section-head"><div><p className="eyebrow">QUICK ACCESS</p><h2>Run your day</h2></div></div><div className="quick-grid"><button data-testid="quick-inventory-btn" onClick={()=>setTab("Inventory")}><Package/><span><b>Update inventory</b><small>{products.length} products need your attention</small></span><ArrowUpRight/></button><button data-testid="quick-order-btn" onClick={()=>setTab("Orders")}><ShoppingBag/><span><b>Place an order</b><small>Order for a customer in seconds</small></span><ArrowUpRight/></button><button data-testid="quick-customer-btn" onClick={()=>setTab("Customers")}><Users/><span><b>View customers</b><small>{customers.length} customers across societies</small></span><ArrowUpRight/></button></div><div className="section-head recent"><div><p className="eyebrow">RECENT ACTIVITY</p><h2>Latest orders</h2></div><button data-testid="view-all-orders-btn" className="text-btn" onClick={()=>setTab("Orders")}>View all <ArrowUpRight size={15}/></button></div><OrderList orders={orders.slice(0,3)}/></>}
function Inventory({products,search,setSearch,onAdd,onEdit}){const [stockValues,setStockValues]=useState({});const valueFor=p=>stockValues[p.id]??p.stock;const saveStock=async(p,value)=>{const next=Math.max(0,Number(value)||0);setStockValues({...stockValues,[p.id]:next});await axios.put(`${API}/products/${p.id}`,{...p,stock:next});};return <><div className="toolbar"><div className="search"><Search size={17}/><input data-testid="inventory-search-input" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search products"/></div><button data-testid="add-product-btn" className="primary-btn" onClick={onAdd}><Plus size={17}/> Add product</button></div><div className="product-grid">{products.map(p=><div className="product-card" data-testid={`product-card-${p.id}`}><div className="product-art"><span>{p.category==="Dairy"?"🥛":p.category==="Produce"?"🍌":"🍞"}</span><i className={p.available?"available":"unavailable"}/></div><div className="product-info"><small>{p.category}</small><h3>{p.name}</h3><div className="product-price-row"><b>{money(p.price)}</b><span className={valueFor(p)<10?"low":"stock"}>{valueFor(p)} in stock</span></div><div className="stock-editor"><button data-testid={`stock-decrease-${p.id}`} aria-label={`Decrease ${p.name} stock`} onClick={()=>saveStock(p,valueFor(p)-1)}>−</button><input data-testid={`stock-quantity-${p.id}`} aria-label={`${p.name} stock quantity`} type="number" min="0" value={valueFor(p)} onChange={e=>setStockValues({...stockValues,[p.id]:e.target.value})} onBlur={e=>saveStock(p,e.target.value)}/><button data-testid={`stock-increase-${p.id}`} aria-label={`Increase ${p.name} stock`} onClick={()=>saveStock(p,valueFor(p)+1)}>+</button></div><button data-testid={`edit-product-${p.id}`} className="edit-link" onClick={()=>onEdit(p)}>Edit price & availability <ArrowUpRight size={13}/></button></div></div>)}</div></>}
function Customers({customers,society,setSociety,onAdd,onEdit}){const list=customers.filter(c=>society==="All societies"||c.society===society);return <><div className="toolbar"><div className="filter-tabs">{["All societies","Prestige Ozone","Palm Meadows","Brigade Gateway","Sobha Dream Acres","Godrej Woods"].map(s=><button data-testid={`society-filter-${s.toLowerCase().replaceAll(" ","-")}`} className={society===s?"selected":""} onClick={()=>setSociety(s)}>{s}</button>)}</div><button data-testid="add-customer-btn" className="primary-btn" onClick={onAdd}><Plus size={17}/> Add customer</button></div><div className="customer-list">{list.map(c=><div className="customer-row" data-testid={`customer-row-${c.id}`}><div className="avatar soft">{c.name.split(" ").map(x=>x[0]).join("")}</div><div className="customer-main"><b>{c.name}</b><span>{c.phone} · {c.address}</span></div><span className="society-tag"><MapPin size={13}/>{c.society}</span><button data-testid={`edit-customer-${c.id}`} className="icon-btn" onClick={()=>onEdit(c)}><ArrowUpRight size={17}/></button></div>)}</div></>}
function Orders({orders,customers,products,selected,setSelected,cart,setCart,cartItems,total,placeOrder,load}){const [statusBusy,setStatusBusy]=useState("");const changeStatus=async(id,status)=>{setStatusBusy(id);await axios.patch(`${API}/orders/${id}/status`,null,{params:{status}});await load();setStatusBusy("")};return <><div className="order-layout"><section className="order-builder"><div className="section-head"><div><p className="eyebrow">NEW ORDER</p><h2>Shop for a customer</h2></div></div><label>Select customer</label><select data-testid="customer-select" value={selected} onChange={e=>setSelected(e.target.value)}><option value="">Choose customer</option>{customers.map(c=><option value={c.id}>{c.name} · {c.society}</option>)}</select><label>Add products</label><div className="order-products">{products.map(p=><div className="order-product"><span>{p.name}<small>{money(p.price)} · {p.stock} available</small></span><div className="stepper"><button data-testid={`decrease-${p.id}`} onClick={()=>setCart({...cart,[p.id]:Math.max(0,(cart[p.id]||0)-1)})}>−</button><b>{cart[p.id]||0}</b><button data-testid={`increase-${p.id}`} disabled={p.stock<=0||cart[p.id]>=p.stock} onClick={()=>setCart({...cart,[p.id]:Math.min(p.stock,(cart[p.id]||0)+1)})}>+</button></div></div>)}</div></section><div className="order-summary"><p className="eyebrow">ORDER SUMMARY</p><h3>{selected?customers.find(c=>c.id===selected)?.name:"No customer selected"}</h3>{cartItems.length?<>{cartItems.map(p=><div className="summary-line"><span>{p.name} × {p.quantity}</span><b>{money(p.price*p.quantity)}</b></div>)}<div className="total"><span>Total</span><b>{money(total)}</b></div><button data-testid="place-order-btn" className="primary-btn full" onClick={placeOrder}>Place order <ArrowUpRight size={17}/></button></>:<p className="empty-summary">Choose a customer and add products to see the total.</p>}</div></div><div className="section-head recent"><div><p className="eyebrow">ORDER HISTORY</p><h2>Recent orders</h2></div></div><OrderList orders={orders} changeStatus={changeStatus} statusBusy={statusBusy}/></>}
function OrderList({orders,changeStatus,statusBusy}){return <div className="order-list">{orders.length?orders.map(o=><div className="order-row" data-testid={`order-row-${o.id}`}><b>{o.id}</b><span>{o.customer_name}<small>{o.society}</small></span><strong>{money(o.total)}</strong><select data-testid={`order-status-${o.id}`} className={`status-select ${o.status.toLowerCase().replaceAll(" ","-")}`} disabled={statusBusy===o.id} value={o.status} onChange={e=>changeStatus(o.id,e.target.value)}><option>Pending</option><option>Out for Delivery</option><option>Delivered</option></select><span className="date">{new Date(o.created_at).toLocaleDateString("en-IN",{day:"2-digit",month:"short"})}</span></div>):<div className="empty-summary">No orders yet.</div>}</div>}
function Modal({title,close,children}){return <div className="modal-backdrop"><div className="modal"><button data-testid="modal-close-btn" className="modal-close" onClick={close}><X/></button><p className="eyebrow">GROVE / NEW</p><h2>{title}</h2>{children}</div></div>}
function FormProduct({form,setForm,submit}){return <div className="form-grid">{[["name","Product name","text"],["price","Price (₹)","number"],["stock","Stock quantity","number"]].map(([k,l,t])=><label>{l}<input data-testid={`product-${k}-input`} type={t} value={form[k]||""} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label>Category<select data-testid="product-category-input" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Dairy</option><option>Produce</option><option>Bakery</option><option>Pantry</option></select></label><label className="availability-toggle"><span>Available for ordering</span><input data-testid="product-availability-input" type="checkbox" checked={form.available!==false} onChange={e=>setForm({...form,available:e.target.checked})}/></label><button data-testid="save-product-btn" className="primary-btn full" onClick={submit}>Save product <Check size={16}/></button></div>}
function FormCustomer({form,setForm,submit}){return <div className="form-grid">{[["name","Full name"],["phone","Phone number"],["address","Address"]].map(([k,l])=><label>{l}<input data-testid={`customer-${k}-input`} value={form[k]||""} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label>Society<select data-testid="customer-society-input" value={form.society||"Prestige Ozone"} onChange={e=>setForm({...form,society:e.target.value})}><option>Prestige Ozone</option><option>Palm Meadows</option><option>Brigade Gateway</option><option>Sobha Dream Acres</option><option>Godrej Woods</option></select></label><button data-testid="save-customer-btn" className="primary-btn full" onClick={submit}>Save customer <Check size={16}/></button></div>}
export default App;